{% extends 'basefront1.html.twig' %}

{% block title %}Etudes{% endblock %}

{% block body %}
    <h1>Etudes</h1>

    <table class="table">
        <tbody>
            
            <tr>
                <th>diplome</th>
                <td>{{ etude.diplome }}</td>
            </tr>
            <tr>
                <th>Emplacement</th>
                <td>{{ etude.emplacement }}</td>
            </tr>
            <tr>
                <th>Date_deb</th>
                <td>{{ etude.dateDeb ? etude.dateDeb|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Date_fin</th>
                <td>{{ etude.dateFin ? etude.dateFin|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_etudes_index') }}">back to list</a>

    <a href="{{ path('app_etudes_edit', {'id': etude.id}) }}">edit</a>

    {{ include('etudes/_delete_form.html.twig') }}
{% endblock %}
